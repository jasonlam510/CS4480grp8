---
description: General style and safety rules for CS4480grp8.
globs:
  - "**/*"
alwaysApply: false
---

**Style**:
- Prefer small, composable modules over monolithic scripts
- Preserve existing directory structure
- Document non-obvious code in comments

**Security**:
- Never hardcode API keys or secrets
- Use environment variables or `.env` files (gitignored)
